# i18n / SEO — статус і план робіт

Документ фіксує, що вже виправлено (частковий фікс) і що лишилося зробити для
повноцінної архітектури двомовності та SEO.

> **Оновлення 2026-06-21:** архітектурні пункти **6, 7, 9, 10 закриті** —
> перейшли на SSR/SSG-локалізацію за URL (`/` для uk, `/en/…` для en), єдиний
> механізм перекладу через словники `public/lang/*.json` + серверний `t()`,
> per-locale meta/og/hreflang/canonical, sitemap з обома локалями та CI-перевірку
> паритету ключів. Деталі — у розділі [«✅ Реалізовано»](#-реалізовано-повна-архітектура-2026-06-21) нижче.

---

## ✅ Зроблено (частковий фікс)

Виправлені баги, що були видимі користувачу або псували словники:

| № | Проблема | Статус |
|---|----------|--------|
| 1 | Іконки зникали при перекладі (`textContent` затирав `<i>`) | ✅ `setTranslatedText` зберігає дочірні елементи |
| 2 | Рік у підвалі «застигав» на 2026 | ✅ рік динамічний, переклад лише суфікса |
| 3 | Адреса схлопувалась в один рядок + друкарські помилки | ✅ `\n` → `<br>`, виправлено пробіл і кирилицю |
| 4 | Українські описи публікацій — заглушки «Текст/Тест» | ✅ додано `pub1_desc`/`pub4_desc`, виправлено розмітку |
| 5 | HTML-теги всередині словника | ✅ теги прибрано зі словника, sanitize-хак видалено |
| 8 | Мертві ключі (~30) у словниках | ✅ видалено після перевірки grep |
| 11 | Дублі фільтрів + плутані імена ключів | ✅ унікальні `data-filter`, перейменовано ключі |
| 12 | Мертвий код `setNavHandlers` | ✅ видалено |
| 13 | Переклад `value` через `setAttribute` | ✅ `value` прибрано зі списку атрибутів |
| 14 | Рассинхрон HTML і словника (`map_header`) | ✅ узгоджено |

**Архітектура перекладу не змінювалась** — лишилась клієнтська схема
`keyed || direct || original` (`apps/web/public/scripts/app.js`). Виправлено лише
розбіжності між розміткою та словником.

---

## ⏳ Лишилось (потребує архітектурного рішення)

### 6 + 7. Єдиний підхід до перекладу, повний словник

**Зараз:** дві несумісні схеми одночасно —
- ключі словника (`data-translate="key"`): головна, about, contacts, publications, reports;
- інлайн (`data-translate-en="…"`): news, events, donate, join.

Через це `uk.json` тримається «наполовину»: система працює українською лише тому,
що вихідний HTML вже українською і спрацьовує фолбек `original`. Будь-яке
розходження HTML ↔ словник дає баг (саме звідси були пункти 2–4).

**Що зробити (вибрати ОДИН підхід):**

**Варіант A — повні словники (рекомендовано для SSR-i18n нижче).**
1. Винести весь текст у `uk.json` + `en.json` (зараз ~137 ключів у розмітці проти 52 в `uk.json`).
2. Прибрати інлайн `data-translate-en` — лишити тільки `data-translate="key"`.
3. Текст у розмітці лишити як SSR-дефолт, що збігається з `uk.json` (захист від FOUC).
4. Додати CI-перевірку «паритету ключів»: скрипт, що порівнює набір ключів у
   `uk.json` і `en.json` і падає на розбіжності (див. нижче приклад).

**Варіант B — лишити лише інлайн `data-translate-{lang}`** на всіх сторінках,
видалити `*.json`. Простіше, але не масштабується на 3+ мови і дублює текст у кожному файлі.

> Рекомендація: **Варіант A**, бо він — передумова для пунктів 9–10.

---

### 9. `<title>` / meta / `<html lang>` не перекладаються

**Зараз:** заголовок, `og:title`, `description`, `og:locale=uk_UA`, `<html lang>`
рендеряться на сервері українською (`Base.astro`) і ніколи не оновлюються при
переключенні на EN. Погано для SEO та шарингу.

**Що зробити:**
- Передавати в `Base.astro` локалізовані `title`/`description` (об'єкт `{ uk, en }` від сторінки).
- При клієнтському переключенні мови у `app.js` оновлювати `document.title`,
  `<meta name="description">`, `og:title/description/locale`, `document.documentElement.lang`.
- Краще — рендерити правильні meta одразу на сервері для кожного мовного URL (див. п.10),
  тоді клієнтське оновлення стає необов'язковим.

---

### 10. Тільки клієнтський переклад → FOUC + немає окремих URL для EN

**Зараз:** при збереженому `en` сторінка спершу малюється українською, потім
мигає на англійську (FOUC). Англійської версії як окремого URL не існує — пошуковики
не індексують EN-контент, немає `hreflang`.

**Що зробити — перейти на SSR/SSG-локалізацію за URL:**

1. **Маршрути за локаллю.** Astro i18n routing:
   ```js
   // astro.config.mjs
   import { defineConfig } from "astro/config";
   export default defineConfig({
     site: "https://avku.org",
     base: "/",
     i18n: {
       defaultLocale: "uk",
       locales: ["uk", "en"],
       routing: { prefixDefaultLocale: false }, // uk: /about, en: /en/about
     },
   });
   ```
2. **Структура сторінок:** `src/pages/[...]` для `uk` + `src/pages/en/[...]` (або
   динамічний `getStaticPaths` по локалях), увесь текст — через `t(key, locale)`.
3. **`hreflang` у `<head>`** (`Base.astro`):
   ```html
   <link rel="alternate" hreflang="uk" href="https://avku.org/about" />
   <link rel="alternate" hreflang="en" href="https://avku.org/en/about" />
   <link rel="alternate" hreflang="x-default" href="https://avku.org/about" />
   ```
4. **Коректні per-locale meta:** `<html lang>`, `og:locale` (`uk_UA` / `en_US`),
   `og:url`, `canonical` — рахувати з поточної локалі.
5. **Перемикач мови** перетворити з кнопок `data-lang` на посилання на той самий
   шлях в іншій локалі (зберігати вибір у cookie для редіректу з кореня).
6. **Прибрати FOUC:** після SSR-локалізації клієнтський `app.js` стає легасі —
   або видалити, або лишити лише для збереження бажаної мови та редіректу.
7. **`sitemap.xml`** — додати обидві локалі з `hreflang`-анотаціями (`@astrojs/sitemap` з `i18n`).

---

## Приклад CI-перевірки паритету ключів (для п.6–7)

```js
// scripts/check-i18n.mjs
import fs from "node:fs";
const uk = JSON.parse(fs.readFileSync("public/lang/uk.json", "utf8"));
const en = JSON.parse(fs.readFileSync("public/lang/en.json", "utf8"));
const a = new Set(Object.keys(uk)), b = new Set(Object.keys(en));
const onlyUk = [...a].filter((k) => !b.has(k));
const onlyEn = [...b].filter((k) => !a.has(k));
if (onlyUk.length || onlyEn.length) {
  console.error("Key mismatch:", { onlyUk, onlyEn });
  process.exit(1);
}
console.log("i18n keys in sync:", a.size);
```

---

## Рекомендований порядок виконання

1. п.6–7 (повні словники + єдиний підхід + CI-перевірка) — фундамент.
2. п.10 (SSR-локалізація за URL + hreflang).
3. п.9 (per-locale meta) — фактично закривається разом з п.10.

---

## ✅ Реалізовано (повна архітектура, 2026-06-21)

### Маршрутизація за локаллю (п.10)
- `astro.config.mjs`: `i18n` з `defaultLocale: "uk"`, `locales: ["uk","en"]`,
  `routing.prefixDefaultLocale: false` → uk без префікса, en із `/en/…`.
- Кожна сторінка винесена в спільний компонент `src/components/pages/*Page.astro`
  з пропом `lang`; тонкі обгортки в `src/pages/<page>.astro` (uk) та
  `src/pages/en/<page>.astro` (en). Розмітка живе в одному місці.
- Перемикач мови в `Header.astro` — це **посилання** на той самий шлях в іншій
  локалі (uk↔en), активний стан рендериться на сервері (`aria-current`).
- Текст рендериться **на сервері**: FOUC немає, EN-контент індексується.

### Єдиний механізм перекладу (п.6–7)
- Словники `public/lang/uk.json` + `public/lang/en.json` — однаковий набір ключів
  (362), однакові значення лише різними мовами. UK більше не тримається на
  фолбеку `original`.
- Серверний `t(key)` у `src/i18n/ui.ts` (читає словники на етапі збірки). У
  розмітці лишається `data-translate="key"`, але текст підставляє `t()`, тож
  SSR-дефолт завжди збігається зі словником.
- Інлайн `data-translate-en="…"` повністю прибрано (0 згадок у `src/` і в `dist/`).
- Клієнтський `public/scripts/app.js` зведено до допоміжних задач (запам'ятати
  вибір мови + одноразовий редірект із кореня) — рушій перекладу видалено.
- Динамічний контент (новини, звіти, партнери, кампанії, архів подій) рендериться
  за `lang` із двомовних даних у фронтметері (а не через словник UI).

### Per-locale SEO/meta (п.9)
Усе виставляє `src/layouts/Base.astro` за поточною локаллю:
`<html lang>`, `<title>`, `<meta description>`, `og:title/description/url/image`,
`og:locale` (`uk_UA`/`en_US`) + `og:locale:alternate`, `canonical`,
`hreflang` для `uk` / `en` / `x-default`.

### Sitemap (п.5)
- `src/pages/sitemap.xml.ts` генерує обидві локалі з `xhtml:link`-анотаціями
  hreflang. Застарілий `public/sitemap.xml` видалено.

### Перевірка паритету (п.2)
- `scripts/check-i18n.mjs` + npm-скрипти `check:i18n` і `prebuild`
  (падає на розбіжності ключів / порожніх значеннях).

### Перевірки, що пройшли
`npm run check:i18n` ✓ · `npm run build` ✓ (20 сторінок + sitemap) ·
0 `data-translate-en` у `src/` та `dist/` · усі `/en/*` згенеровано ·
EN-HTML містить англійський текст і коректні title/meta/lang/og/hreflang одразу.
